trigger CEPayableTrigger on McaApp__Payable__c (after delete, after insert, after update) {
/*
  CEPayableTriggerHandler PayableHandler = new CEPayableTriggerHandler(); 
  
  if(trigger.isAfter){
    
    if(trigger.isInsert){
      PayableHandler.OnAfterInsert(trigger.oldMap, trigger.new);
    }
    
    if(trigger.isUpdate){
      PayableHandler.OnAfterUpdate(trigger.old, trigger.new);
    } 
    
    if(trigger.isDelete){
      PayableHandler.OnAfterDelete(trigger.old, trigger.new);
    }
  }*/

}